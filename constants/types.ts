export interface Song {
    title: string;
    artist: string;
}

export interface ContextData {
    vibe: string;
    weather: string;
    temperature: number;
    location: string;
    time: string;
}
